# Copy this file to .env (gitignored) and fill in real values.
# Never commit a file named .env with real values in it.

# Google Maps SDK key for Android — restrict it in Google Cloud Console to:
#   package name: com.rajankumar2327.emergencyhelpnetwork
#   SHA1 fingerprint: (get yours with: eas credentials, or keytool -list -v -keystore your.jks)
GOOGLE_MAPS_API_KEY=
